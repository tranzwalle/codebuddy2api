# codebuddy2api 使用文档

把本机已登录的 **CodeBuddy / WorkBuddy（腾讯代码助手）** 订阅，转成本地 **OpenAI / Anthropic 兼容 API**。转换器只做协议转换和鉴权注入，不负责登录，也不替你执行工具。

项目目录：`/Users/quan/dev/quant/codebuddy2api`

---

## 1. 前置条件

1. 本机已安装并登录 **CodeBuddy** 或 **WorkBuddy** 桌面端
2. Python 3.8+（本机已用 `uv` 创建 `.venv`）
3. 依赖已安装：`fastapi`、`uvicorn`、`httpx`

macOS 登录态默认路径：

```text
~/Library/Application Support/CodeBuddyExtension/Data/Public/auth/*.info
```

目录里若有多个 `.info` 文件，转换器会按文件名排序取第一个。可用环境变量覆盖搜索目录：

```bash
export CODEBUDDY_AUTH_DIR="/你的/auth目录"
```

---

## 2. 启动与停止

### 一键启动

```bash
cd /Users/quan/dev/quant/codebuddy2api
./start.sh
```

脚本会自动：进入项目目录、创建 `.venv`、安装依赖、检测端口；若 `http://127.0.0.1:8787/health` 已是本服务则直接退出。默认带 `--desensitize --log converter.log`。

```bash
./start.sh --daemon          # 后台运行，PID 写入 converter.pid
./start.sh --port 8788       # 8787 被占用时换端口
./start.sh -- --api-key x    # 额外参数传给 converter.py
./start.sh --stop            # 停止本脚本拉起的后台进程
```

等价的手动命令：

```bash
uv run converter.py --desensitize --log converter.log
```

看到类似下面的输出即表示已就绪：

```text
✅ 监听 http://127.0.0.1:8787
   GET  /v1/models
   POST /v1/chat/completions
   POST /v1/responses
   POST /v1/messages
   GET  /health
```

| 参数 | 含义 |
|------|------|
| `--desensitize` | 压缩 / 脱敏 agent 运行时提示，降低腾讯后端内容审核误拦 |
| `--log converter.log` | 把请求摘要和错误写到项目目录下的日志文件（已在 `.gitignore`） |

等价写法：

```bash
source .venv/bin/activate
python3 converter.py --desensitize --log converter.log
```

### 常用参数

| 参数 | 默认 | 说明 |
|------|------|------|
| `--host` | `127.0.0.1` | 监听地址，仅本机访问 |
| `--port` | `8787` | 监听端口；被占用时改成 `8788` 等 |
| `--api-key` | 无 | 给本地客户端加一层校验；不设则不校验 |
| `--no-compact` | 关 | 配合 `--desensitize`：只做零宽脱敏，保留更完整的 system prompt |
| `--skip-check` | 否 | 跳过启动时的登录态预检 |

端口被占用时：

```bash
uv run converter.py --desensitize --log converter.log --port 8788
```

此时客户端里的 Base URL 也要改成对应端口。

### 停止

前台启动用 `Ctrl+C`。后台启动用：

```bash
./start.sh --stop
```

或查找并结束进程：

```bash
lsof -nP -iTCP:8787 -sTCP:LISTEN
kill <PID>
```

---

## 3. 接口一览

默认 Base URL：`http://127.0.0.1:8787`

| 方法 | 路径 | 用途 |
|------|------|------|
| `GET` | `/health` | 健康检查、登录态摘要 |
| `GET` | `/v1/models` | 模型列表 |
| `GET` | `/api.json` | Kimi Code custom registry（同 `/v1/models/api.json`） |
| `POST` | `/v1/chat/completions` | OpenAI Chat Completions（Cherry Studio、ZCode、SDK 等） |
| `POST` | `/v1/responses` | OpenAI Responses（Codex CLI） |
| `POST` | `/v1/messages` | Anthropic Messages（Claude Code / CC Switch） |

支持流式 SSE、原生 `tools` / `tool_calls`、多轮工具调用。

`/v1/models` 和 `/api.json` 展示的模型列表**可以自定义**（见下一节）。但即使列表里没写，只要腾讯后端支持，你也可以在请求里直接填真实 `model` 名称调用。

模型名必须填腾讯侧真实名称，不会把 `gpt-4o`、`claude-sonnet-4` 之类自动映射过去。

---

## 3.1 如何添加 / 更新模型列表

腾讯后端**没有公开的 models 拉取接口**，所以本服务无法自动和官方客户端完全同步。你需要自己维护展示列表。

### 方式一：项目内 `models.json`（推荐）

```bash
cp models.json.example models.json
```

编辑 `models.json`，两种写法都支持：

```json
{
  "defaults": {
    "reasoning": true,
    "support_efforts": ["low", "medium", "high"],
    "default_effort": "medium"
  },
  "models": [
    "glm-5.2",
    "deepseek-v4-flash",
    {
      "id": "新模型-id",
      "name": "新模型展示名",
      "tool_call": true,
      "reasoning": true,
      "support_efforts": ["low", "medium", "high"],
      "default_effort": "medium"
    }
  ]
}
```

Kimi Code 的思考模式依赖 `api.json` 里的 `reasoning` 或 `support_efforts` 字段。只写模型 ID 字符串时，默认会为所有模型开启思考，并附带 `support_efforts: ["low", "medium", "high"]`。

也可用 `defaults` 给整份列表设默认值；单个模型对象里的字段会覆盖 `defaults`。若某个模型不需要思考，可单独设置 `"reasoning": false`。

保存后**重启服务**。启动日志会显示模型来源，例如：

```text
模型列表  : 12 个（来源 file:/Users/quan/dev/quant/codebuddy2api/models.json）
```

`/health` 里也有 `models_source` 和 `model_count` 字段。

### 方式二：复用 CodeBuddy CLI 的 `~/.codebuddy/models.json`

若你已在官方 CLI 里配置过模型，且本目录没有 `models.json`，服务会自动读取：

```text
~/.codebuddy/models.json
```

该文件支持官方格式（`models` + `availableModels`）。参考 [CodeBuddy models.json 文档](https://www.codebuddy.ai/docs/cli/models)。

### 方式三：环境变量（快速覆盖）

```bash
export CODEBUDDY2OPENAI_MODELS="glm-5.2,deepseek-v4-flash,新模型-id"
./start.sh
```

逗号分隔，会**完全替换**内置列表。

### 方式四：指定配置文件路径

```bash
./start.sh -- --models-file /path/to/my-models.json
# 或
export CODEBUDDY2OPENAI_MODELS_FILE=/path/to/my-models.json
```

### 模型 ID 从哪里找？

1. 看官方 CodeBuddy / WorkBuddy 客户端模型下拉里显示的名称（通常就是后端 `model` 字段）
2. 在客户端里发一次对话，用开发者工具或 `--log converter.log` 看实际 `model` 值
3. 直接试：列表里没有的模型，也可以先写进 `models.json` 或请求体里测试

### Kimi Code 更新模型后

修改 `models.json` 并重启服务，然后重新导入 registry：

```bash
kimi provider remove codebuddy
kimi provider add http://127.0.0.1:8787/api.json --api-key any-value
```

若思考模式仍不可用，检查 `curl http://127.0.0.1:8787/api.json` 里对应模型是否包含 `reasoning` 或 `support_efforts` 字段。

---

## 4. 快速自检

```bash
curl http://127.0.0.1:8787/health
curl http://127.0.0.1:8787/v1/models
curl http://127.0.0.1:8787/api.json
```

`/health` 里 `status` 应为 `ok`，且 `credential.token_expired` 为 `false`。

非流式对话：

```bash
curl http://127.0.0.1:8787/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"deepseek-v4-flash","messages":[{"role":"user","content":"你好"}]}'
```

流式：

```bash
curl -N http://127.0.0.1:8787/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"glm-5.2","stream":true,"messages":[{"role":"user","content":"数1到5"}]}'
```

若启动时加了 `--api-key mysecret`，请求需带：

```bash
-H "Authorization: Bearer mysecret"
```

---

## 5. 客户端接入

以下示例默认端口为 `8787`。若你改了 `--port`，同步替换。

### 5.1 Cherry Studio / ZCode / LobeChat / NextChat / Open WebUI

在「OpenAI 兼容」提供商中填写：

| 项 | 值 |
|----|----|
| Base URL | `http://127.0.0.1:8787/v1` |
| API Key | 留空；若转换器加了 `--api-key` 则填同一个值 |
| 模型 | `glm-5.2` 或 `deepseek-v4-flash` 等 |

### 5.2 Codex CLI

转换器走 `/v1/responses`。把下面片段合并进 `~/.codex/config.toml`（不要覆盖整个文件）：

```toml
[model_providers.workbuddy]
name = "WorkBuddy (via local converter)"
base_url = "http://127.0.0.1:8787/v1"
wire_api = "responses"
env_key = "CODEBUDDY2OPENAI_KEY"

[profiles.workbuddy]
model = "glm-5.2"
model_provider = "workbuddy"
```

```bash
export CODEBUDDY2OPENAI_KEY=any-value
codex --profile workbuddy "你的任务描述"
```

Codex / Claude Code 场景建议始终加 `--desensitize`。若仍被审核拦截，可试：

```bash
uv run converter.py --desensitize --no-compact --log converter.log
```

更完整的配置片段见仓库内 `codex-codebuddy.example.toml`。

### 5.3 Claude Code / CC Switch

Claude Code 走 Anthropic 协议，不要填 OpenAI 的 `/v1`。

```json
{
  "DeepSeek-V4-Pro": {
    "base_url": "http://127.0.0.1:8787/v1/messages",
    "api_key": "",
    "model": "deepseek-v4-pro"
  }
}
```

### 5.4 OpenAI Python SDK

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://127.0.0.1:8787/v1",
    api_key="not-needed",
)

resp = client.chat.completions.create(
    model="glm-5.2",
    messages=[{"role": "user", "content": "你好"}],
)
print(resp.choices[0].message.content)
```

### 5.5 Kimi Code（custom registry）

Kimi Code 支持通过 `api.json` 批量导入 provider。本服务已提供：

- `http://127.0.0.1:8787/api.json`
- `http://127.0.0.1:8787/v1/models/api.json`（同上）

先启动服务，再执行：

```bash
kimi provider add http://127.0.0.1:8787/api.json --api-key any-value
```

说明：

- 未启用 `--api-key` 时，`--api-key` 可填任意值（Kimi CLI 要求提供该参数）
- 若启动时加了 `--api-key mysecret`，这里必须填同一个值
- 导入后 provider ID 为 `codebuddy`，模型别名形如 `codebuddy/glm-5.2`
- 在 TUI 里用 `/model` 或 `-m codebuddy/glm-5.2` 选择模型

也可通过环境变量导入：

```bash
KIMI_REGISTRY_API_KEY=any-value kimi provider add http://127.0.0.1:8787/api.json
```

若服务跑在非默认端口，把 URL 里的端口一并改掉。反向代理场景可设置：

```bash
export CODEBUDDY2OPENAI_BASE_URL="https://your-domain.example/v1"
```

这样 `api.json` 里的 `api` 字段会指向该地址，而不是本机 `127.0.0.1`。

---

## 6. 环境变量

| 变量 | 作用 |
|------|------|
| `CODEBUDDY_AUTH_DIR` | 指定登录态目录（目录内需有 `*.info`） |
| `CODEBUDDY2OPENAI_KEY` | 等同启动参数 `--api-key` |
| `CODEBUDDY2OPENAI_BASE_URL` | `api.json` 中 `api` 字段的公开地址（反向代理时使用） |
| `CODEBUDDY2OPENAI_MODELS` | 逗号分隔模型 ID，覆盖内置 / 文件列表 |
| `CODEBUDDY2OPENAI_MODELS_FILE` | 指定模型列表 JSON 文件路径 |
| `CODEBUDDY2OPENAI_LOG` | 未传 `--log` 时的日志路径 |

---

## 7. 排障

| 现象 | 处理 |
|------|------|
| 启动提示未找到登录文件 | 先在桌面端完成登录，确认 auth 目录下有 `*.info` |
| 本地 `401` | 启用了 `--api-key`，但客户端没带同一个 key |
| 后端 `401` | token 失效，重新打开 CodeBuddy / WorkBuddy 登录 |
| `502` / `upstream error: 403` | 转换器访问 `https://copilot.tencent.com` 被拦；确认进程有外网权限，不要在无出站网络的沙箱里跑 |
| 端口已被占用 | `lsof -nP -iTCP:8787 -sTCP:LISTEN`，结束旧进程或改 `--port` |
| 响应慢 | 换更快的模型，例如 `deepseek-v4-flash` |
| 被「敏感内容」拦截 | 开 `--log`，看同一请求 ID 下的 `REQUEST BODY`；Codex / Claude Code 开 `--desensitize`，仍不稳再加 `--no-compact` |
| 模型名无效 | 必须用上一节列出的腾讯侧名称，不能填 `gpt-4o` |

日志里每次请求有唯一 ID，常见字段：

- `REQUEST BODY`：发往后端的实际内容
- `RESPONSES PROJECTION`：Codex 请求压缩前后的消息数 / 字符数
- `⚠️内容审核拦截`：命中腾讯审核

审核误拦常见触发词包括 `DoS`、`exploit`、`credential`、`sandbox`、竞争品牌词，以及 tool description 里的安全术语——不一定是你的用户问题本身敏感。

---

## 8. Docker（可选）

需要把宿主机登录态目录挂进容器：

```bash
docker compose up -d --build
```

或：

```bash
docker build -t workbuddy2api .
docker run -d --name workbuddy2api -p 8787:8787 \
  -v "$HOME/Library/Application Support/CodeBuddyExtension/Data/Public/auth:/data/auth:ro" \
  -e CODEBUDDY_AUTH_DIR=/data/auth \
  workbuddy2api
```

日常本机使用直接跑 `uv run converter.py` 即可，不必上 Docker。

---

## 9. 注意

- 本项目仅用于个人学习与研究，请在你合法拥有订阅的前提下使用。
- 不要把 `*.info`、token、`converter.log` 提交到 git。
- 转换器进程需要能访问 `https://copilot.tencent.com`；桌面端保持登录，token 临近过期时会自动刷新并回写 auth 文件。
